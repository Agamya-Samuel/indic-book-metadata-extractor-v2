<?php
# LocalSettings.Extensions.php
# Extension configuration values that should be set after bundled extensions are loaded.
# This file is required by LocalSettings.php line 155.
# The Wikibase Docker image entrypoint is supposed to generate this automatically,
# but we provide a stub so the container doesn't crash if generation fails.

# Add post-extension MediaWiki/Wikibase configuration here.
