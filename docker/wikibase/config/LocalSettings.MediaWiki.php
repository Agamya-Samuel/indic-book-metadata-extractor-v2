<?php
# LocalSettings.MediaWiki.php
# MediaWiki configuration values that must be set before bundled extensions are loaded.
# This file is required by LocalSettings.php line 149.
# The Wikibase Docker image entrypoint is supposed to generate this automatically,
# but we provide a stub so the container doesn't crash if generation fails.

# Add pre-extension MediaWiki configuration here.
