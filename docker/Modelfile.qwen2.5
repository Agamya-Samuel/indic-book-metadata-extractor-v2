FROM qwen2.5:7b

# Qwen2.5 7B Instruct — Apache 2.0 licensed, multilingual (29+ languages).
# Replaces Airavata (AI4Bharat 7B Q4_K_M) as the default extraction model.
# Qwen2.5 handles JSON-schema output more reliably than Airavata Q4_K_M,
# producing fewer all-null batches and better instruction following.
#
# Uses Ollama's native Qwen2.5 chat template (do NOT override — Qwen2.5's
# <|im_start|>/<|im_end|> template differs structurally from Airavata's LLaMA
# <s> template). We only tune sampler + context window here.

PARAMETER temperature 0.3
PARAMETER num_ctx 8192
