# =============================================================================
# Dokploy Production Compose — Indic Book Metadata Extractor
# =============================================================================
# How to use:
#   1. In Dokploy → Docker Compose service → set "Compose Path" to this file
#   2. Set env vars in Dokploy's "Environment" tab (see .env.production.example)
#   3. Click Deploy — everything else is automatic
# =============================================================================

services:

  # ─────────────────────────────────────────────────────────────────────────────
  # Database
  # ─────────────────────────────────────────────────────────────────────────────
  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_DB: indic_books
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./docker/postgres/init_extensions.sql:/docker-entrypoint-initdb.d/init_extensions.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ─────────────────────────────────────────────────────────────────────────────
  # Cache + Message Broker
  # ─────────────────────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: >
      redis-server
      --appendonly yes
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
      --save 60 1000
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ─────────────────────────────────────────────────────────────────────────────
  # LLM Inference (Airavata 7B)
  # ─────────────────────────────────────────────────────────────────────────────
  ollama:
    image: ollama/ollama:latest
    restart: unless-stopped
    volumes:
      - ollama_data:/root/.ollama
      - ./docker/ollama-entrypoint.sh:/entrypoint.sh
      - ./docker/Modelfile.airavata:/models/Modelfile.airavata
    entrypoint: ["/bin/bash", "/entrypoint.sh"]
    environment:
      OLLAMA_PRELOAD_MODEL: airavata
      OLLAMA_KEEP_ALIVE: "30m"
    deploy:
      resources:
        limits:
          memory: 8G
    healthcheck:
      test: ["CMD-SHELL", "ollama list > /dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s

  # ─────────────────────────────────────────────────────────────────────────────
  # FastAPI Backend
  # ─────────────────────────────────────────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: ../docker/Dockerfile.backend
    restart: unless-stopped
    expose:
      - "8000"
    environment:
      # App
      APP_NAME: ${APP_NAME:-Indic Book Metadata Extractor}
      DEBUG: ${DEBUG:-false}
      CORS_ORIGINS: ${CORS_ORIGINS:-*}
      MAX_UPLOAD_SIZE_MB: ${MAX_UPLOAD_SIZE_MB:-200}
      # Internal services (fixed — never change in Docker)
      DATABASE_URL: postgresql+asyncpg://postgres:${POSTGRES_PASSWORD}@postgres:5432/indic_books
      REDIS_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/1
      OLLAMA_URL: http://ollama:11434
      STORAGE_PATH: /app/storage
      TESSERACT_CMD: tesseract
      DEFAULT_OCR_LANGUAGE: ${DEFAULT_OCR_LANGUAGE:-tel}
      # New Relic (disabled by default — set NEW_RELIC_ENABLED=true + key to enable)
      NEW_RELIC_ENABLED: ${NEW_RELIC_ENABLED:-false}
      NEW_RELIC_LICENSE_KEY: ${NEW_RELIC_LICENSE_KEY:-}
      NEW_RELIC_APP_NAME: ${NEW_RELIC_APP_NAME:-Indic Book Metadata Extractor}
      NEW_RELIC_ENVIRONMENT: ${NEW_RELIC_ENVIRONMENT:-production}
      NEW_RELIC_DISTRIBUTED_TRACING_ENABLED: ${NEW_RELIC_DISTRIBUTED_TRACING_ENABLED:-true}
      NEW_RELIC_LOG: ${NEW_RELIC_LOG:-error}
      NEW_RELIC_CONFIG_FILE: /app/newrelic.ini
      NEW_RELIC_MONITOR_MODE: ${NEW_RELIC_MONITOR_MODE:-true}
      NEW_RELIC_DEVELOPER_MODE: ${NEW_RELIC_DEVELOPER_MODE:-false}
    volumes:
      - storage_data:/app/storage
      - ./backend/app:/app/app
      - ./backend/alembic:/app/alembic
      - ./backend/alembic.ini:/app/alembic.ini
      - ./docker/scripts/backend-entrypoint.sh:/entrypoint.sh:ro
    entrypoint: ["/bin/bash", "/entrypoint.sh"]
    command: ["uv", "run", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      ollama:
        condition: service_healthy

  # ─────────────────────────────────────────────────────────────────────────────
  # Celery Worker (OCR + LLM tasks)
  # ─────────────────────────────────────────────────────────────────────────────
  worker:
    build:
      context: ./backend
      dockerfile: ../docker/Dockerfile.backend
    restart: unless-stopped
    command: ["uv", "run", "celery", "-A", "app.tasks.celery_app", "worker", "--loglevel=info", "--concurrency=4", "-Q", "ocr,llm,default"]
    environment:
      # Internal services (fixed)
      DATABASE_URL: postgresql+asyncpg://postgres:${POSTGRES_PASSWORD}@postgres:5432/indic_books
      REDIS_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/1
      OLLAMA_URL: http://ollama:11434
      STORAGE_PATH: /app/storage
      TESSERACT_CMD: tesseract
      DEFAULT_OCR_LANGUAGE: ${DEFAULT_OCR_LANGUAGE:-tel}
      # New Relic
      NEW_RELIC_ENABLED: ${NEW_RELIC_ENABLED:-false}
      NEW_RELIC_LICENSE_KEY: ${NEW_RELIC_LICENSE_KEY:-}
      NEW_RELIC_APP_NAME: ${NEW_RELIC_APP_NAME:-Indic Book Metadata Extractor Worker}
      NEW_RELIC_ENVIRONMENT: ${NEW_RELIC_ENVIRONMENT:-production}
      NEW_RELIC_DISTRIBUTED_TRACING_ENABLED: ${NEW_RELIC_DISTRIBUTED_TRACING_ENABLED:-true}
      NEW_RELIC_LOG: ${NEW_RELIC_LOG:-error}
      NEW_RELIC_CONFIG_FILE: /app/newrelic.ini
      NEW_RELIC_MONITOR_MODE: ${NEW_RELIC_MONITOR_MODE:-true}
      NEW_RELIC_DEVELOPER_MODE: ${NEW_RELIC_DEVELOPER_MODE:-false}
    volumes:
      - storage_data:/app/storage
      - ./backend/app:/app/app
      - ./backend/alembic:/app/alembic
      - ./backend/alembic.ini:/app/alembic.ini
    deploy:
      resources:
        limits:
          memory: 2G
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      ollama:
        condition: service_healthy

  # ─────────────────────────────────────────────────────────────────────────────
  # Next.js Frontend
  # ─────────────────────────────────────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: ../docker/Dockerfile.frontend
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/api}
        NEXT_PUBLIC_OPENREFINE_URL: ${NEXT_PUBLIC_OPENREFINE_URL:-}
        NEXT_PUBLIC_NEW_RELIC_LICENSE_KEY: ${NEXT_PUBLIC_NEW_RELIC_LICENSE_KEY:-}
        NEXT_PUBLIC_NEW_RELIC_APP_ID: ${NEXT_PUBLIC_NEW_RELIC_APP_ID:-}
        NEXT_PUBLIC_NEW_RELIC_ACCOUNT_ID: ${NEXT_PUBLIC_NEW_RELIC_ACCOUNT_ID:-}
        NEW_RELIC_ENABLED: ${NEW_RELIC_ENABLED:-false}
        NEW_RELIC_LICENSE_KEY: ${NEW_RELIC_LICENSE_KEY:-}
        NEW_RELIC_APP_NAME: ${NEW_RELIC_APP_NAME:-Indic Book Metadata Extractor}
    restart: unless-stopped
    expose:
      - "3000"
    environment:
      NEXT_PUBLIC_OPENREFINE_URL: ${NEXT_PUBLIC_OPENREFINE_URL:-}
      NEW_RELIC_ENABLED: ${NEW_RELIC_ENABLED:-false}
      NEW_RELIC_LICENSE_KEY: ${NEW_RELIC_LICENSE_KEY:-}
      NEW_RELIC_APP_NAME: ${NEW_RELIC_APP_NAME:-Indic Book Metadata Extractor}
    depends_on:
      - backend

  # ─────────────────────────────────────────────────────────────────────────────
  # Celery Flower (task monitor)
  # ─────────────────────────────────────────────────────────────────────────────
  flower:
    image: mher/flower:latest
    restart: unless-stopped
    expose:
      - "5555"
    environment:
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/1
      FLOWER_PORT: "5555"
      FLOWER_BASIC_AUTH: ${FLOWER_BASIC_AUTH}
    depends_on:
      redis:
        condition: service_healthy

  # ─────────────────────────────────────────────────────────────────────────────
  # Daily DB Backup (retains last 7)
  # ─────────────────────────────────────────────────────────────────────────────
  backup:
    image: postgres:16-alpine
    restart: unless-stopped
    volumes:
      - ./docker/scripts/backup-db.sh:/backup-db.sh:ro
      - backup_data:/backups
    environment:
      PGHOST: postgres
      PGUSER: postgres
      PGPASSWORD: ${POSTGRES_PASSWORD}
      PGDATABASE: indic_books
    entrypoint: >
      /bin/sh -c "
      apk add --no-cache bash &&
      while true; do
        /backup-db.sh
        sleep 86400
      done
      "
    depends_on:
      postgres:
        condition: service_healthy

  # ─────────────────────────────────────────────────────────────────────────────
  # Wikibase (structured data store)
  # ─────────────────────────────────────────────────────────────────────────────
  wikibase:
    image: wikibase/wikibase:mw1.44.0
    restart: unless-stopped
    expose:
      - "80"
    environment:
      DB_SERVER: mysql:3306
      DB_USER: wikibase
      DB_PASS: ${MYSQL_PASSWORD}
      DB_NAME: wikibase
      MW_ADMIN_NAME: admin
      MW_ADMIN_PASS: ${WIKIBASE_ADMIN_PASS}
      MW_ADMIN_EMAIL: admin@localhost
      MW_WG_SERVER: "http://${WIKIBASE_HOST:-wikibase}"
      MW_WG_SITENAME: IndicBooks
      MW_WG_LANGUAGE_CODE: en
      WIKIBASE_PUBLIC_HOST: ${WIKIBASE_HOST:-wikibase}
      WIKIBASE_PUBLIC_SCHEME: ${WIKIBASE_SCHEME:-http}
      ELASTICSEARCH_HOST: elasticsearch
      METADATA_CALLBACK: "false"
      MW_SECRET_KEY: ${MW_SECRET_KEY:-}
      MW_UPGRADE_KEY: ${MW_UPGRADE_KEY:-}
    volumes:
      - ./docker/wikibase/config:/config:z
      - ./docker/wikibase/config/LocalSettings.MediaWiki.php:/LocalSettings.MediaWiki.php
      - ./docker/wikibase/config/LocalSettings.Extensions.php:/LocalSettings.Extensions.php
      - wikibase_data:/var/www/html/images
    depends_on:
      mysql:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy

  mysql:
    image: mariadb:10.11
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: wikibase
      MYSQL_USER: wikibase
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5

  elasticsearch:
    image: wikibase/elasticsearch:1
    restart: unless-stopped
    environment:
      discovery.type: single-node
      ES_JAVA_OPTS: "-Xms512m -Xmx512m"
    volumes:
      - elasticsearch_data:/usr/share/elasticsearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200/_cluster/health | grep -q '\"status\"' || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 60s

  # ─────────────────────────────────────────────────────────────────────────────
  # OpenRefine (bulk data cleaning GUI)
  # ─────────────────────────────────────────────────────────────────────────────
  openrefine:
    build:
      context: ./docker/openrefine
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "3333"
    volumes:
      - openrefine_data:/refine

volumes:
  pgdata:
  redis_data:
  ollama_data:
  storage_data:
  backup_data:
  wikibase_data:
  mysql_data:
  elasticsearch_data:
  openrefine_data:
